<% content_for :title, "Editing task" %>

<div class="container">
    <%= link_to "<< Back to project", project_path(@project) %>

    <div class="column">
        <div class="row">

            <h1>Editing task</h1>

            <%= render "form", task: @task %>

            <br>

            <div>
                <%= link_to "<< Back to project", project_path(@project) %>
            </div>

        </div>
    </div>
</div>